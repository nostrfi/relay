<Project>
    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))"/>
    <PropertyGroup>
        <AssemblyName>Nostrfi.Models</AssemblyName>
        <RootNamespace>Nostrfi.Models</RootNamespace>
    </PropertyGroup>
</Project>