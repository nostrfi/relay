# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/backend/relay
/backend/loadtest

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.txt
coverage.html

# Go workspace file
go.work
go.work.sum

# Dependency directories
vendor/

# DuckDB database files
backend/db/*.db
backend/db/*.db.wal

# Environment and configuration files
# Unanchored, so it covers both frontend/.env and the repository-root .env
# that `docker compose` reads NUXT_SESSION_PASSWORD from.
frontend/.env
.env.local
config.local.yaml

# Nuxt dev/build outputs
frontend/.output
frontend/.data
frontend/.nuxt
frontend/.nitro
frontend/.cache
frontend/dist
frontend/node_modules

# JetBrains IDEs
.idea/
*.iml
*.iws
.idea_modules/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Linux
*~
.directory

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
